import os
import numpy as np
import argparse

if __name__ == '__main__':
    parser = argparse.ArgumentParser()
    parser.add_argument('--file_path', type=str, default='../AllAssignments')
    parser.add_argument('--numGTA', type=int, default=4)
    parser.add_argument('--')
    parser.add_argument('-- ')     
    parser.add_argument('-- ')        
    parser.add_argument('-- ')
    parser.add_argument('-- ')
    parser.add_argument('-- ')
    parser.add_argument('-- ') assd 

    config = parser.parse_args()
